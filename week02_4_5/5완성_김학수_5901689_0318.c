/*
#include <stdio.h>

int main() {
정수형 변수 a, b, result = 0; //a, b는 입력한 두 값 . reuslt는 결과값
문자형 변수 p; // + , - , / , *

    입력함수(두 수와 사칙연산의 기호를 입력 받는 함수 구현)

    if(연산이 덧셈인지 검증하는 조건) {
        입력한 두 수를 합하는 기능을 구현한다.
    } else if (연산이 뺄셈인지 검증하는 조건) {
        입력한 두 수의 차를 구하는 기능을 구현한다. 
    } else if (연산이 나눗셈인지 검증하는 조건) {
        입력한 두 수에 대해 나눗셈을 하는 기능을 구현한다. 
    } else if (연산이 곱셈인지 검증하는 조건) {
        입력한 두 수를 곱하는 기능을 구현한다. 
    }
    출력 함수(계산된 결과를 출력한다.)

}
*/